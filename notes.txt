updatear nueva database server
_cambiar nombre de proyecto en package.json al actual

.correr watch sass abajo a la derecha para detectar cambios

abajo de eso poner un mini history explicando cuales uso, practico, etc..... cuando se clickee el boton

getFeatures().. se ejecuta cada vez que apreto un boton o paso a mobile, deberia ejecutarse solo una vez que es cuando el html dinamico se poner
hay problema relacionad con waitForFeatures() en features.js, el await hace que se ejecute varias veces la promesa de getFeatures() 
o cuando se le llama a waitForFeatures() con .then().